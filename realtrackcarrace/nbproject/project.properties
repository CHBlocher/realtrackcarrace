applet.draggable=true
applet.height=200
applet.width=200
application.title=TRACK
application.vendor=Diego
build.classes.excludes=**/*.java,**/*.form,**/*.fx
build.classpath=\
    ${javac.classpath}:\
    ${src.dir}:\
    ${build.classes.dir}:
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
debug.classpath=\
    ${run.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/TRACK.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
execution.target=standard
file.reference.JDLayout_v2.2.1.jar=externaljar/JDLayout_v2.2.1.jar
file.reference.memefx.jar=externaljar/memefx.jar
file.reference.phys2d-060408.jar=externaljar/phys2d-060408.jar
includes=**
jar.compress=true
javac.classpath=\
    ${file.reference.JDLayout_v2.2.1.jar}:\
    ${file.reference.memefx.jar}:\
    ${file.reference.phys2d-060408.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javadoc.additionalparam=
javadoc.author=true
javadoc.encoding=
javadoc.private=true
javadoc.version=true
javafx.profile=desktop
jnlp.codebase.type=local
jnlp.codebase.url=file:/C:/Documents%20and%20Settings/Diego/Documenti/NetBeansProjects/TRACK/dist/
jnlp.offline-allowed=true
jnlp.packEnabled=false
jnlp.signed=true
main.class=Main
pack200.jar.compress=true
platform.active=default_fx_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=\ -Djava.security.policy=java.policy 
source.encoding=UTF-8
src.dir=src
